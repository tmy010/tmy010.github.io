import { EventDates } from '@/const/event'

export type TImageWithCountdown = {
    title: string
    eventDate: EventDates
    eventTextColor
}

const ImageWithCountdown = ({}: TImageWithCountdown) => {
    return <></>
}

export default ImageWithCountdown
